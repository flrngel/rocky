# Companion note for `sl-promote.json`

This spec targets the autonomous promotion path, where `/teach` is only setup. The load-bearing behavior is Rocky promoting a candidate policy during a normal `run_prompt` after verified reuse.
